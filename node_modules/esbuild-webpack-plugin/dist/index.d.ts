import { Compiler } from 'webpack';
import { TransformOptions, TransformResult } from 'esbuild';
export interface ESBuildPluginOptions extends Omit<TransformOptions, 'minify' | 'sourcemap' | 'sourcefile'> {
}
export default class ESBuildPlugin {
    private readonly options;
    private static service;
    constructor(options?: ESBuildPluginOptions);
    static ensureService(enforce?: boolean): Promise<void>;
    transformCode({ source, file, devtool, }: {
        source: string;
        file: string;
        devtool: string | boolean | undefined;
    }): Promise<TransformResult>;
    apply(compiler: Compiler): void;
}
